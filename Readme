import { TestBed } from '@angular/core/testing';
import { DebugViewComponent } from './debug-view.component'; // Import the standalone component
import { HttpClientTestingModule } from '@angular/common/http/testing';
import { provideHttpClient } from '@angular/common/http';
import { MatSelectModule } from '@angular/material/select'; // For Material dropdown
import { DxDataGridModule } from 'devextreme-angular'; // DevExtreme grid

describe('DebugViewComponent', () => {
  beforeEach(async () => {
    await TestBed.configureTestingModule({
      imports: [DebugViewComponent, HttpClientTestingModule, MatSelectModule, DxDataGridModule], // ✅ Add standalone component in imports
      providers: [provideHttpClient()], // ✅ Provide HttpClient for services
    }).compileComponents();
  });

  it('should create the component', () => {
    const fixture = TestBed.createComponent(DebugViewComponent);
    const component = fixture.componentInstance;
    expect(component).toBeTruthy();
  });
});
